language: php
dist: trusty
php:
  - 7.0
  - 7.1
  - hhvm
  - nightly

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

matrix:
  fast_finish: true
  allow_failures:
    - php: nightly

before_script:
    - rm -f composer.lock
    - composer install --prefer-dist --ignore-platform-reqs

script:
  ./vendor/bin/phpunit tests/

git:
  depth: 5
